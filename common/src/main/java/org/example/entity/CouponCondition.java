package org.example.entity;

public enum CouponCondition {
    AllProduct("AllProduct"),
    ParticularProduct("ParticularProduct"),
    ParticularShopProduct("ParticularShopProduct");

    CouponCondition(String couponCondition) {
    }
}
